<%= form_for(@credit) do |f| %>
  <% if @credit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@credit.errors.count, "error") %> prohibited this credit from being saved:</h2>

      <ul>
      <% @credit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Arkadas_No %><br />
    <%= f.text_field :friend_id ,:readonly=>'true' %>
  </div>
  <div class="field">
    <%= f.label :miktar %><br />
    <%= f.text_field :miktar %>
  </div>
  <div class="field">
    <%= f.label :durum %><br />
    <%=  select("credit", "durum", STATUS_CHOICES.collect{|key,value| [value, key]})  %>
  </div>
  <div class="field">
    <%= f.label :Verdigim_Tarih %><br />
    <%= f.date_select :ver_tarih %>
  </div>
  <div class="field">
    <%= f.label :Vade_Tarihi %><br />
    <%= f.date_select :vade_tarih %>
  </div>
  <div class="actions">
    <p>&nbsp;</p>
    <%= f.submit(label='Kaydet') %>

  </div>
<% end %>
